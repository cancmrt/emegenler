language: csharp
mono: none
dotnet: 3.0

addons:
  sonarcloud:
    organization: "$ORGANIZATION"

before_install:
    - bash ./tools/travis-ci-install-sonar.sh
install:
    - dotnet restore
script:
    - bash ./tools/travis-ci-build.sh 