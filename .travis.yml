language: csharp
solution: EmegenlerSolution.sln
install:
  - nuget restore EmegenlerSolution.sln
  - nuget install xunit.runners -Version 1.9.2 -OutputDirectory testrunner
script:
  - msbuild /p:Configuration=Release EmegenlerSolution.sln